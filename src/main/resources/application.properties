# Spring Boot Configuration
spring.application.name=translation-call-app
server.port=8080

# Database Configuration (H2 for simplicity, can be changed to PostgreSQL for production)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Twilio Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID:ACcc6f219caba396964d06e1c322aa9ed2}
twilio.auth.token=${TWILIO_AUTH_TOKEN:f7f70e729b197403bcc617135f920161}
twilio.phone.number=${TWILIO_PHONE_NUMBER:+17817859042}

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:your-openai-api-key-here}

# Application URLs - Use Railway domain instead of localhost
app.base.url=${APP_BASE_URL:https://talktranslate-backend-production.up.railway.app}
app.websocket.stream-url=${APP_WEBSOCKET_URL:wss://talktranslate-backend-production.up.railway.app/media-stream}

# Logging Configuration
logging.level.com.example.translationcallapp=INFO
logging.level.com.twilio=DEBUG
logging.level.org.springframework.web=INFO

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Server Configuration
server.servlet.context-path=/
server.error.include-stacktrace=never